/**
 * @file gate_packed_ch.c
 * @brief CH (controlled-Hadamard) gate for mixed states in packed lower-triangular storage
 */

#include "gate.h"

void ch_packed(state_t *state, const qubit_t control, const qubit_t target) {
    GATE_VALIDATE(0, "ch: packed not yet implemented");
}
